using UnityEngine;
using System.Collections;


public class GameControlScript : MonoBehaviour {





	void OnGUI()
	{
		
		/*for(int i = 0, j = 0; i < ranking.players.GetLength(0); i++, j+=20)
		{
			GUI.Label (new Rect(10, 10 + j, 200, 400), ranking.players[i, 0] + " / " + ranking.players[i, 1]);
		}*/

	}
}
