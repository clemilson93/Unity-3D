using UnityEngine;
using System.Collections;

public class SCDemo : MonoBehaviour {

	// Use this for initialization
	void Start ()
    {
        for (int inteiro = 0; inteiro < 5; inteiro ++)
        {
            print(inteiro);
        }
    }

    // Update is called once per frame
    void Update ()
    {
        
	}
}
